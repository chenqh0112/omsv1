# 独立站 - 美国货盘页 PRD

## 页面的数据来源

- 当前本地原型从 `sku-data.js` 读取商品图片、名称、规格、SKU编码、货盘总量、单价、可售数量、状态、今日成交额和库龄记录。
- “我的订单”从 `order-data.js` 的本地模拟订单读取，并由 `order-history.js` 渲染；购物车和表格选择状态仅保存在当前页面内存中。
- 正式环境应由对公货盘查询服务返回当前用户可见的商品、价格和可售库存，由OMS/订单服务提供库存状态、预计到货、历史订单和物流信息。

## 1. 页面定位

美国货盘页是对公界面的表格型选品入口，用于按商品和 SKU 变体查看可销售货盘、筛选库存、比较价格与库龄，并把单个或批量 SKU 加入购物车。

页面入口位于左侧导航“对公界面 > 美国货盘”。

## 2. 数据范围

页面根据当前身份展示可见货盘数据：

| 身份 | 可见数据范围 |
| --- | --- |
| 超管 | 全部公司和业务组的对公货盘 |
| 公司A管理员 | 公司A范围内的对公货盘 |
| 公司A业务组A管理员 | 公司A业务组A范围内的对公货盘 |
| 公司A业务组A业务员 | 业务员A本人权限范围内的对公货盘 |

切换身份后，概览数据、列表、结果数量、已选 SKU 和购物车同步刷新。切换身份会清空原身份下的选择和购物车，避免跨权限保留商品。

## 3. 页面结构

页面由以下区域组成：

1. 顶部栏：页面标题、“我的订单”图标和身份切换头像。
2. 概览区：货盘SKU总数、货盘总量、可售数量、今日成交额。
3. 筛选区：SKU编码、SKU名称、状态、搜索、重置和购物车摘要。
4. 货盘总览表格：按商品聚合多个 SKU 变体。
5. 购物车弹窗：维护已选 SKU 和数量。
6. 图片预览：查看商品大图。

### 3.1 我的订单

- 顶部右侧的订单图标展示当前登录用户的历史订单数量。
- 点击图标打开“我的订单”弹窗，仅展示当前登录用户下过的订单。
- 列表展示订单号、下单时间、SKU明细、订单金额、订单状态和物流单号。
- 点击“查看全部订单”进入订单管理页，并自动按当前用户名筛选订单。
- 历史订单状态与管理端统一为待审核、待分配仓库、待仓库拣货、待物流收揽、已发货、商品缺货、已关闭和拦截发货成功。

## 4. 概览区

| 指标 | 数值口径 | 占比口径 |
| --- | --- | --- |
| 货盘SKU总数 | 当前身份可见 SKU 数 | 当前身份可见 SKU 数 / 全部货盘 SKU 数 |
| 货盘总量 | 当前身份可见 SKU 的货盘数量之和 | 当前身份货盘总量 / 全部货盘总量 |
| 可售数量 | 当前身份可见 SKU 的可售数量之和 | 可售数量 / 当前身份货盘总量 |
| 今日成交额 | 当前身份可见 SKU 的今日成交额之和 | 当前身份今日成交额 / 全部货盘今日成交额 |

每个概览模块展示占比、趋势方向和对比周期。问号图标用于说明占比或趋势计算方式。

概览区只随身份变化，不随当前筛选条件变化。

## 5. 筛选区

| 筛选项 | 控件 | 规则 |
| --- | --- | --- |
| SKU编码 | 文本输入框 | 支持部分匹配，不区分大小写 |
| SKU名称 | 文本输入框 | 支持部分匹配，不区分大小写 |
| 状态 | 可搜索多选下拉 | 全部、缺货、偏少、充足，默认选择全部 |

交互规则：

- SKU编码、SKU名称和状态之间为“且”关系。
- 状态选择多个值时，选项之间为“或”关系。
- 点击“搜索”或在文本输入框按 Enter 后刷新列表。
- 点击“重置”清空文本条件、恢复状态“全部”、取消排序并回到第一页。
- 状态选项发生变化时立即刷新列表。

## 6. 购物车摘要

筛选区右侧展示：

- 小计：购物车内全部 SKU 的购买件数之和。
- 合计：每个 SKU 的单价乘以数量后求和。
- 购物车图标：展示当前购物车总件数，点击打开购物车弹窗。

小计和合计在加入商品、调整数量、删除商品或清空购物车后即时更新。

## 7. 商品聚合规则

同名商品聚合为一个商品组，每个商品组下展示多个 SKU 变体。例如“极光随行保温杯”的不同容量和颜色在同一组中展示。

商品组汇总行展示：

- 商品图片和商品名称。
- 变体数量。
- 货盘总量合计。
- 单价区间。
- 可售数量合计。
- 商品组状态。
- 最长库龄。

商品组下方逐行展示每个 SKU 变体的规格、SKU编码、数量、单价、状态、库龄和操作。

商品组状态按风险最高的变体确定：

1. 任意变体为缺货，商品组状态为缺货。
2. 无缺货但任意变体为偏少，商品组状态为偏少。
3. 全部变体充足，商品组状态为充足。

## 8. 货盘总览表格

### 8.1 字段顺序

| 字段 | 定义 |
| --- | --- |
| 选择 | 选择商品组或单个可售 SKU |
| SKU | 商品图片、商品名称、规格和SKU编码 |
| 货盘总量 | 当前对公货盘中的件数 |
| 单价 | 当前 SKU 的对公销售单价 |
| 可售数量 | 当前仍可购买的件数 |
| 状态 | 充足、偏少或缺货 |
| 库龄 | SKU 最大库龄，商品组显示组内最长库龄 |
| 操作 | 每个 SKU 独立提供加入购物车和下单 |

除 SKU 列外，数量、状态、库龄和操作内容居中展示。

### 8.2 排序

以下表头支持升序和降序切换：

- 货盘总量：按商品组内变体数量合计排序。
- 单价：按商品组内变体平均单价排序。
- 可售数量：按商品组内可售数量合计排序。
- 状态：按缺货、偏少、充足的风险顺序排序。

首次点击数量或价格字段默认降序；首次点击状态默认从缺货开始。切换排序后回到第一页。

### 8.3 状态展示

| 状态 | 颜色 | 操作状态 |
| --- | --- | --- |
| 充足 | 绿色 | 可选择、可加入购物车、可点击下单 |
| 偏少 | 黄色 | 可选择、可加入购物车、可点击下单 |
| 缺货 | 红色 | 选择框、加入购物车和下单按钮全部不可用 |

鼠标移入或键盘聚焦“缺货”标签时，展示预计到货日期。

### 8.4 图片预览

- 点击商品组图片打开大图预览。
- 预览展示商品名称。
- 点击遮罩、关闭按钮或按 Escape 关闭预览。

## 9. 选择与批量加入购物车

选择支持三个层级：

1. 表头选择框：选择当前页全部可售 SKU。
2. 商品组选择框：选择该商品组下全部可售变体。
3. SKU 选择框：只选择当前 SKU。

选择规则：

- 缺货 SKU 不可选择。
- 商品组部分 SKU 被选择时，商品组选择框显示半选状态。
- 当前页部分 SKU 被选择时，表头选择框显示半选状态。
- 选中至少一个 SKU 后，“添加购物车”按钮可用。
- 批量加入成功后清空表格选择状态，并显示大尺寸 Toast“已加入购物车”。Toast 内提供“查看购物车”链接，点击直接打开购物车弹窗。

## 10. 行内操作

| 操作 | 页面行为 |
| --- | --- |
| 加入购物车 | 将当前 SKU 以 1 件加入购物车；已存在时数量加 1，但不超过可售数量 |
| 下单 | 当前页面显示 Toast“下单功能暂未开放” |

缺货 SKU 的两个操作按钮均置灰不可点击。

## 11. 购物车弹窗

购物车内每个 SKU 展示：

- 商品图片。
- SKU名称、规格和SKU编码。
- 单价。
- 数量输入框。
- 删除按钮。

数量规则：

- 最小值为 1。
- 最大值为当前 SKU 可售数量。
- 输入空值、0 或非法值时恢复为 1。
- 输入超过可售数量时自动调整为可售上限。

弹窗底部展示总件数和总金额。

| 操作 | 页面行为 |
| --- | --- |
| 删除 | 删除当前 SKU，显示 Toast“已移出购物车” |
| 清空购物车 | 删除购物车全部商品，显示 Toast“购物车已清空” |
| 下单 | 显示 Toast“下单功能暂未开放” |

## 12. 分页与空状态

- 页面按商品组分页，每页展示 5 个商品组。
- 分页区展示当前筛选结果的 SKU 数和商品组数。
- 上一页和下一页按钮在边界页自动不可用。
- 筛选、重置、排序或身份切换后回到第一页。
- 没有匹配数据时，表格显示“暂无匹配的SKU”。

## 13. 页面状态与反馈

- 页面初始化展示当前身份权限范围内的全部货盘数据。
- 操作反馈统一使用顶部大尺寸 Toast；购物车添加成功的 Toast 提供可点击的“查看购物车”引导链接。
- 图片预览和购物车弹窗支持 Escape 关闭。
- 注释区域默认打开；黄色“查看注释”入口支持拖动并记忆位置，注释面板宽度可调整。

## 14. 数据字段

| 字段 | 用途 |
| --- | --- |
| `barcode` | SKU编码和唯一标识 |
| `name` | 商品名称及商品聚合依据 |
| `spec` | SKU规格 |
| `img` | 商品图片 |
| `company` | 所属公司 |
| `businessGroup` | 所属业务组 |
| `salesperson` | 所属业务员 |
| `trayQuantity` | 货盘总量 |
| `unitPrice` | 对公单价 |
| `available` | 可售数量 |
| `sellableStatus` | 充足、偏少或缺货 |
| `todayRevenue` | 今日成交额 |
| `ageRecords` | 库龄记录数组 |

购物车金额计算公式为：`合计 = Σ（SKU单价 × SKU购买数量）`。

## 15. 概览趋势计算

趋势只跟随当前身份可见的对公货盘范围，不随SKU编码、SKU名称、状态筛选或表格排序变化。

通用百分比公式：`趋势百分比 =（当前值 - 对比值）/ 对比值 × 100%`。

| 指标 | 当前值 | 对比周期 |
| --- | --- | --- |
| 货盘SKU总数 | 当前时点可见且对外开放的去重SKU数 | 7天前同一时点的可见去重SKU数 |
| 货盘总量 | 当前时点可见SKU的货盘总量之和 | 7天前同一时点的货盘总量之和 |
| 可售数量 | 当前时点可见SKU的可售数量之和 | 7天前同一时点的可售数量之和 |
| 今日成交额 | 当日00:00至当前时点已支付且未关闭订单的商品金额之和 | 前一日00:00至相同时间点的对应成交额 |

- 前三项展示“vs 上周”，今日成交额展示“vs 昨日”。
- 趋势结果按页面现有整数百分比展示；正值使用向上箭头，负值使用向下箭头。
- 对比值为0且当前值为0时展示 `0%`；对比值为0且当前值大于0时展示“新增”。
- 当前页面的 `6%`、`9%`、`3%`、`14%` 为原型示例值，正式环境通过货盘历史快照和订单支付流水计算。
