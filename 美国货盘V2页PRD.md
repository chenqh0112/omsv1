# 独立站 - 美国货盘V2页 PRD

## 1. 页面定位

美国货盘V2是对公采购用户的卡片型选品页面，重点提供直观的商品浏览、SKU 搜索、采购数量填写、购物车维护和采购订单提交能力。

页面入口位于左侧导航“对公界面 > 美国货盘V2”。

## 2. 页面结构

页面由以下区域组成：

1. 商城主导航：美国货盘品牌、紧凑型商品名称搜索框、购物车图标、“我的订单”图标和登录用户信息。
2. 商城快捷导航：全部商品分类入口。
3. 商品分类：点击“全部商品”展开左侧大类、右侧商品图阵的联动分类面板。
4. 商品控制区：美国现货标题、库存状态分段筛选、排序方式和批量添加到购物车。
5. 商品结果区：支持多选的大图 SKU 商品卡片网格。
6. 分页区：上一页、当前页和下一页。
7. 浮动购物车：已选 SKU 数和总金额。
8. 购物车弹窗：修改数量、删除商品、清空和进入结算。
9. 结算弹窗：填写收货信息、邮编、快递公司、上传面单并提交订单。
10. 账户内容区：我的订单、我的收藏和地址管理均在商城顶栏下方整页展示。
11. 图片预览：查看 SKU 大图。

## 3. 商城顶栏

顶部内容在同一行展示：

| 位置 | 内容 |
| --- | --- |
| 左侧 | “US”品牌标记、页面名称“美国货盘”和英文辅助标识 |
| 中间 | 支持商品名称、规格或SKU编码的紧凑搜索框、橙色搜索按钮；桌面端宽度约为原长搜索框的一半 |
| 右侧 | 购物车图标、“我的订单”图标、登录用户头像、用户标识“登录用户”、用户名“采购用户A”和菜单箭头 |

点击登录用户信息展开菜单，菜单包含“我的收藏”“地址管理”和“退出登录”。点击“退出登录”跳转至登录页。

“我的订单”图标展示当前用户的历史订单数量。点击后保持商城顶栏不变，将下方商品区域切换为整页订单列表，仅展示当前用户下过的订单，包括订单号、下单时间、SKU明细、订单金额、订单状态和物流信息。每条订单支持打开详情，查看商品、收货信息和费用明细。

“我的订单”左侧显示购物车图标与购物车 SKU 数量。点击后打开与右下角浮动购物车相同的购物车弹窗，两处数量保持同步。

“批量添加到购物车”位于综合排序右侧，展示当前选中的SKU数量。未选中SKU时按钮置灰，选中至少一个SKU后可点击。

顶栏下方提供商品分类入口：

- 点击“全部商品”展开左右联动分类面板。左侧展示推荐分类、保温杯、运动水壶、吸管杯、办公通勤和家庭儿童；点击左侧分类后右侧商品图阵同步切换，点击右侧商品后回到列表并按商品名称搜索。

## 4. 搜索

搜索框同时匹配商品名称、规格和SKU编码：

- 支持部分匹配。
- 不区分大小写。
- 点击“搜索”或在输入框按 Enter 执行搜索。
- 搜索后回到第一页。
- 输入为空时恢复全部商品。
- 无匹配结果时显示“没有找到匹配的SKU”。

### 4.1 状态筛选

商品控制区提供“全部、充足、偏少、缺货”四个分段选项：

- 默认选择“全部”。
- 状态条件与搜索关键词之间为“且”关系。
- 点击状态后立即刷新商品卡片并回到第一页。
- 充足、偏少、缺货分别使用绿色、黄色和红色状态点。

### 4.2 排序

排序下拉框支持：

- 综合排序：保持默认商品顺序。
- 价格从低到高。
- 价格从高到低。
- 库存从高到低。

切换排序后立即刷新商品卡片并回到第一页。

## 5. 商品卡片

每张卡片对应一个独立 SKU，展示以下信息：

| 字段 | 展示规则 |
| --- | --- |
| 选择 | 每张卡片左上角仅展示选择框，不显示“选择”文本；选中后卡片显示橙色描边 |
| 产品图片 | 使用卡片主体面积展示大图；点击图片打开大图预览 |
| 收藏 | 图片左下角常驻爱心图标；点击可收藏或取消收藏，收藏结果在“我的收藏”中查看并保留到后续访问 |
| 产品名称 | 最多展示两行，超出时省略 |
| 商品说明 | 展示规格和“美国仓现货”，不展示额外采购宣传文案 |
| SKU编码 | 在库存信息右侧以弱化文本展示，用于识别具体变体 |
| 单价 | 产品名称下方使用大号金额展示，美元金额保留两位小数 |
| 可售数量 | 当前 SKU 最多可购买的件数 |
| 状态 | 充足、偏少或缺货 |
| 数量步进器 | 通过减号、输入框或加号调整本次加入购物车的件数 |
| 加入购物车 | 卡片最右侧显示纯购物车图标按钮，悬停显示“加入购物车”；点击后把当前数量加入购物车 |

商品网格采用类似采购商城的货架式布局：图片优先，名称、商品说明、单价、可售数量和采购操作从上到下排列。数量步进器位于左侧，购物车图标按钮固定在操作区最右侧。

状态颜色：

| 状态 | 颜色 |
| --- | --- |
| 充足 | 绿色 |
| 偏少 | 黄色 |
| 缺货 | 红色 |

缺货 SKU：

- 数量输入框固定为 0 并不可编辑。
- “加入购物车”按钮置灰不可点击。
- 选择框仍可选中；执行批量加入时缺货SKU自动跳过。

## 6. 加入购物车

数量规则：

- 可售 SKU 默认数量为 1。
- 最小可加入数量为 1。
- 最大数量为当前 SKU 可售数量。
- 点击减号或加号时按1件递减或递增。
- 输入空值、0 或非法值时按 1 件处理。
- 输入超过可售数量时按可售上限处理。

加入规则：

- SKU 首次加入时，购物车数量等于卡片输入数量。
- SKU 已在购物车时，在原数量基础上累加。
- 累加后的数量不得超过可售数量。
- 操作完成后显示放大的 Toast“已加入购物车”，Toast 内提供“查看购物车”链接，点击直接打开购物车弹窗。

批量加入规则：

- 支持跨当前分页保留所选SKU和填写的数量。
- 点击“批量添加到购物车”后，将所有有可售库存的所选SKU加入购物车。
- 已在购物车中的SKU累加数量，但不超过可售数量。
- 缺货SKU不进入购物车。
- 完成后清空卡片选择状态，并显示实际加入的SKU数量；成功 Toast 提供“查看购物车”链接。

## 7. 浮动购物车

浮动购物车固定在页面右下角，展示：

- 已选 SKU 种类数。
- 当前购物车总金额。

SKU 种类数按不同 SKU 编码去重，不等同于商品总件数。

注释面板打开时，浮动购物车移动到注释面板左侧，避免被遮挡。点击浮动购物车打开购物车弹窗。

## 8. 购物车弹窗

每个已选 SKU 展示：

| 字段 | 说明 |
| --- | --- |
| SKU名称和规格 | 展示商品名称、规格和SKU编码 |
| 单价 | 当前商品单价 |
| 数量 | 可直接修改 |
| 小计 | 单价乘以当前数量 |
| 删除 | 从购物车移除当前 SKU |

数量修改规则：

- 最小值为 1。
- 最大值为当前可售数量。
- 修改后立即刷新当前行小计、购物车合计和浮动购物车金额。

弹窗操作：

| 操作 | 页面行为 |
| --- | --- |
| 删除 | 移除当前 SKU，显示 Toast“已从购物车删除” |
| 清空 | 清空全部 SKU，显示 Toast“购物车已清空” |
| 去结算 | 购物车非空时打开结算弹窗 |

购物车为空时显示空状态。空购物车点击“去结算”时显示 Toast“购物车为空”。

## 9. 结算流程

结算弹窗包含以下必填字段：

| 字段 | 控件与规则 |
| --- | --- |
| 收货人 | 文本输入框，必填 |
| 联系电话 | 电话输入框，必填 |
| 收货地址 | 多行文本框，必填 |
| 邮编 | 文本输入框，必填 |
| 快递公司 | 下拉选择，必填 |
| 上传面单 | 支持 PDF、JPG、PNG 文件，展示已选择文件名 |

快递公司选项：

- USPS
- FenEx
- UPS
- 燕文物流
- 亚马逊物流
- 顺丰速运
- 圆通速递
- GoFor
- UniUni

结算摘要展示 SKU 种类数、商品总件数、货款、运费、OMS收取费用和应付合计。OMS费用中单独说明操作费和包材费，应付合计为货款、运费与OMS费用之和。

点击“返回购物车”关闭结算弹窗并重新打开购物车。点击“提交订单”后：

- 清空购物车。
- 更新浮动购物车为 0 个 SKU、金额 `$0.00`。
- 关闭结算弹窗。
- 清空收货信息表单与面单文件名。
- 显示 Toast“订单提交成功”，并切换到“我的订单”整页列表。

正式业务环境提交时，需要把商品明细、金额、收货信息和物流方式发送至订单服务，并以后端返回结果决定是否清空购物车。

## 10. 图片预览

- 点击商品卡片图片打开预览。
- 预览显示商品名称和规格。
- 点击遮罩、关闭按钮或按 Escape 关闭预览。

## 11. 分页与可视区域

- 页面宽度充足时每页展示15个SKU，按5列×3行排列。
- 注释面板打开或内容区域较窄时每页展示6个SKU，按3列×2行排列。
- 移动端每页展示6个SKU，按2列×3行排列。
- 分页显示上一页、当前页和下一页。
- 第一页禁用上一页。
- 最后一页或无结果时禁用下一页。
- 搜索后自动返回第一页。
- 商城顶栏固定在内容区顶部，商品货架随页面纵向滚动；完成3行浏览后展示分页。

## 12. 页面状态与反馈

- 页面初始化展示全部可采购 SKU。
- 商品卡片按当前结果顺序排列。
- 所有操作反馈统一使用顶部大尺寸 Toast；购物车添加成功的 Toast 提供可点击的“查看购物车”引导链接。
- 购物车弹窗、结算弹窗和图片预览支持 Escape 关闭。
- 点击弹窗遮罩关闭当前弹窗。
- 黄色“查看注释”按钮支持拖动并记忆位置，注释面板默认打开且宽度可调。

## 13. 数据字段

| 字段 | 用途 |
| --- | --- |
| `barcode` | SKU编码和唯一标识 |
| `name` | 产品名称 |
| `spec` | SKU规格 |
| `img` | 产品图片 |
| `unitPrice` | 商品单价 |
| `available` | 可售数量和数量输入上限 |
| `sellableStatus` | 充足、偏少或缺货 |

购物车数据至少包含：

| 字段 | 用途 |
| --- | --- |
| `item` | SKU 商品信息 |
| `quantity` | 当前购买数量 |

金额计算公式：

- SKU小计：`unitPrice × quantity`
- 购物车合计：`Σ（unitPrice × quantity）`
- 应付合计：`货款 + 运费 + 操作费 + 包材费`
