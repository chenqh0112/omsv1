# 独立站 - 费用明细页 PRD

## 页面的数据来源

用户在前端对公页面里下单后产生的一系列费用明细，需要依赖OMS接口。

## 1. 页面定位

用于查看费用规模、结算进度和 SKU 级计费记录。页面入口位于左侧导航“费用 > 费用明细”。

## 2. 页面结构

页面由顶部栏、费用概览、筛选区、费用明细表格和页面注释组成。

## 3. 费用概览

| 指标 | 统计口径 | 趋势示例 |
| --- | --- | --- |
| 本月费用 | 本月全部费用金额 | ↑11.6% vs 上月同期 |
| 待结算 | 尚未完成结算的金额 | ↑8.3% vs 上月同期 |
| 已结算 | 本月已经完成结算的金额 | ↑19.2% vs 上月同期 |
| 费用单 | 本月生成的费用单数量 | ↑5.0% vs 上月 |

每张卡片左上角同一行展示指标标题与小字号说明。数字使用大字号和指标色，趋势位于数字同一行右侧；内容区域较窄时保留方向和百分比，隐藏较长的对比周期。

## 4. 筛选区

| 筛选项 | 可选值 |
| --- | --- |
| 费用单号 | 文本输入；支持不区分大小写的部分匹配 |
| 费用类型 | 全部类型、操作费、包材费、物流费、仓租费 |
| 费用项目 | 全部项目、仓租费、退货销毁费、退货贴标、退货入库处理费 |
| 结算状态 | 全部状态、待结算、已结算、已冲销 |

筛选条件之间为“且”关系。点击“搜索”或在筛选区按 Enter 刷新表格和结果条数；点击“重置”清空费用单号并恢复其他全部条件。

费用类型与费用项目在同一张费用单的明细行内匹配：一张费用单只要存在同时满足所选类型与项目的明细行，即进入筛选结果。结算状态按费用单状态匹配。

## 5. 费用明细

| 字段 | 说明 |
| --- | --- |
| 费用单号 | 费用单唯一编号，表头和内容左对齐；同时展示聚合的费用项目数 |
| 费用类型 | 操作费、包材费、物流费、仓租费 |
| 费用项目 | 具体收费项目 |
| SKU | OMS返回的SKU编码 |
| 计费说明 | 费用产生原因和计费内容 |
| 单价 | 单位计费价格 |
| 计费数量 | 参与计费的数量 |
| 倍率 | 当前计费倍率 |
| 金额 | 单价、计费数量和倍率的计算结果 |
| 币种 | 当前金额币种 |
| 计费日期 | 费用产生日期 |
| 结算状态 | 待结算、已结算或已冲销 |
| 备注 | 补充计费信息 |

金额计算公式为：`金额 = 单价 × 计费数量 × 倍率`。

同一费用单中的多个 SKU 和费用项目聚合为表格中的一行，各明细字段在单元格内按行对齐展示，金额列展示整张费用单的合计金额。

费用列表以费用单为分页单位，每页10张费用单。分页展示费用单总数、当前页和总页数，筛选后回到第一页。

列表为空时显示“暂无匹配费用单”，总条数为0，分页保持为 `1 / 1`。


## 6. 页面反馈

- 筛选无结果时，结果条数显示为0，表格不展示匹配行。
- 页面业务反馈遵循全局大尺寸 Toast 规范（开发不用管，此功能为原型所有）。

## 8. 概览趋势计算

费用概览基于当前管理权限内的全部费用单计算，不受费用单号、费用类型、费用项目或结算状态筛选条件影响。

百分比公式：`趋势百分比 =（当前值 - 对比值）/ 对比值 × 100%`。

| 指标 | 当前值 | 对比值 |
| --- | --- | --- |
| 本月费用 | 本月1日00:00至当前时点产生的全部费用金额之和 | 上月1日00:00至与当前月相同日、相同时间产生的费用金额之和 |
| 待结算 | 当前时点状态为待结算的费用单金额之和 | 上月同日同一时点处于待结算状态的费用单金额之和 |
| 已结算 | 本月1日00:00至当前时点完成结算的费用金额之和 | 上月1日00:00至相同日、相同时间完成结算的费用金额之和 |
| 费用单 | 本月1日00:00至当前时点生成的费用单数 | 上一个自然月生成的费用单总数 |

- 本月费用、待结算、已结算展示“vs 上月同期”，费用单展示“vs 上月”。
- 趋势保留一位小数。箭头表示数学增减，业务颜色可单独表达好坏；待结算金额下降可使用正向颜色。
- 对比值为0且当前值为0时展示 `0.0%`；对比值为0且当前值大于0时展示“新增”。
- 当前页面的 `11.6%`、`8.3%`、`19.2%`、`5.0%` 为原型演示值，正式环境按费用产生时间、结算时间和历史结算状态计算。

## 9. 费用试算与冲销

- 用户打开结算页、修改地址或快递公司后重新调用费用试算；提交订单前再试算一次，并把货款、运费、操作费、包材费、币种和试算时间保存为订单费用快照。
- 费用试算失败时不允许提交订单，保留购物车并展示失败原因。
- 退货、订单取消或账单更正产生的撤销费用必须新增独立冲销记录，状态为“已冲销”，通过原费用单号关联原记录。
- 原费用记录和冲销记录同时保留，冲销金额以负数表示，不允许覆盖或直接改写原费用。
- 费用创建、结算与冲销均携带本站业务单号；重复请求只能成功一次，并保存失败原因和重试记录。
