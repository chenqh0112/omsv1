# 独立站 - 退货管理页 PRD

## 页面的数据来源

对公页面用户针对订单发起退货申请的数据。

对公买家只负责从已发货订单发起退货申请并提交退货商品、数量、原因和说明，不选择收货后的仓内处理方式，也不申请仓内增值作业。收货后处理方式和增值作业统一由运营在管理端审核时配置。

## 1. 页面定位

退货管理页用于运营审核对公订单及原出库单产生的退货申请，并把仓库收货后的处理方式和可选增值作业作为指令提交给 OMS。页面不再直接执行收货、质检、上架、销毁或退款。

页面入口位于“售后 > 退货管理”。

## 2. 页面结构

页面包含退货概览、筛选区、退货审核列表、分页区、审核弹窗和退货详情弹窗。右侧注释默认展开并加载本PRD。

## 3. 顶部统计

| 指标 | 统计口径 |
| --- | --- |
| 待人工审核订单 | 当前命中规则且审核状态为“待人工审核”的退货单数量 |
| 待审核商品 | 待人工审核订单中全部 SKU 的退货件数合计 |
| 本月退货单 | 本月创建的退货单数量 |
| 本月退货额 | 本月退货单涉及的退款或抵扣金额合计 |

每个指标同时展示与上周或上月的趋势。审核完成后，待人工审核订单和待审核商品即时减少。

## 4. 筛选

- 退货单号和关联订单号支持模糊查询。
- 审核状态：全部、待人工审核、自动通过、人工通过和已拒绝。
- 不同条件之间为“且”关系；搜索或重置后回到第一页。

## 5. 退货审核列表

| 字段 | 说明 |
| --- | --- |
| 退货单号 | 系统生成或 OMS 返回的退货唯一编号，左对齐 |
| 关联订单号 | 对公采购订单或原出库单编号 |
| 退货用户 | 发起退货申请的用户名；列表不展示公司信息 |
| 退货SKU+数量 | 商品图片、名称、SKU种类和退货总件数 |
| 退货原因 | 质量问题、错发、买家拒收或多退 |
| 审核状态 | 待人工审核、自动通过、人工通过或已拒绝 |
| 收货后处理方式 | 待人工审核时显示“审核后确定”；自动或人工通过后显示处理方式；已拒绝显示“不适用” |
| 增值作业 | 展示申请拆箱质检、申请换箱或“无” |
| 退货金额 | 当前退货申请涉及的退款或抵扣金额 |
| 操作 | 待人工审核记录显示“审核”和“详情”；其他记录显示“详情” |

列表按每页10条真实分页，分页展示总条数、当前页和总页数。

## 6. 审核操作

退货管理页只保留“审核”这一项业务操作。仅命中规则而进入待人工审核的记录展示“审核”按钮；未命中规则的申请自动通过，运营无需逐单处理。

### 6.1 收货后处理方式

运营必须选择以下一种处理方式：

| 处理方式 | OMS和仓库执行规则 |
| --- | --- |
| 等仓库收货后再选择 | 仓库完成收货并上传外箱照片后，您可查看外箱情况，再选择入库或销毁 |
| 仓库收货后直接入库 | 无需卖家再次选择，仓库收货后直接入库 |
| 仓库收货后直接销毁 | 无需卖家再次选择，仓库收货后直接销毁 |

默认选择“等仓库收货后再选择”。

### 6.2 增值作业

运营可以选择以下零项、一项或多项增值作业：

- 申请拆箱质检。
- 申请换箱。

增值作业由 OMS 接收后下发仓库。对公买家不展示、不选择也不修改这些选项。

### 6.3 提交结果

点击“确认审核”后：

- 审核通过时状态由“待人工审核”更新为“人工通过”；拒绝时更新为“已拒绝”并保存拒绝原因。
- 保存收货后处理方式、增值作业、审核人和审核时间。
- 将审核结果和处理指令提交 OMS。
- 列表、分页和顶部待审核统计即时更新。
- 显示 Toast“审核完成，处理指令已提交 OMS”。

正式环境只有 OMS 接口返回成功后才更新审核状态；接口失败时保留“待人工审核”并展示失败原因。

### 6.4 退货详情与进度

点击任意退货记录的“详情”打开退货详情弹窗，详情包含关联订单、审核状态、退货用户、退货原因、收货后处理方式、增值作业、退货商品明细、件数和退货金额。

退货进度按以下四个节点展示：

1. 退货申请已提交：展示买家提交日期。
2. 审核判定：待人工审核时显示命中规则并等待运营处理；自动通过、人工通过或已拒绝时展示审核方式、时间和结果。
3. 仓库收货：展示等待仓库收货或仓库已确认收到退件。
4. 收货后处理：展示待执行的处理方式或处理完成结果。


## 7. 管理边界

- 对公买家只发起退货申请，不参与审核及仓内处理配置。
- 管理端运营负责审核并配置处理指令。
- OMS负责创建或接收退货单、把审核指令传递给仓库并同步处理结果。
- 仓库负责实际收货、拆箱质检、换箱、入库或销毁。
- 本页面不提供收货、质检、上架、销毁、退款和修改已审核指令的操作按钮。

## 8. OMS接口需求

正式环境需要 OMS 提供或确认以下能力：

| 接口能力 | 关键内容 |
| --- | --- |
| 创建退货申请 | 接收原订单号、用户、退货SKU、数量、原因和说明，返回 OMS退货单号 |
| 查询退货单 | 返回退货单、原订单、用户、SKU、金额、审核状态和 OMS处理状态 |
| 提交审核指令 | 接收退货单号、收货后处理方式、增值作业、审核人和审核时间 |
| 同步处理结果 | 返回仓库收货、拆箱质检、换箱、入库或销毁的实际执行状态及时间 |

提交审核指令接口必须支持幂等，避免重复点击导致仓库收到重复作业指令。

## 9. 数据字段

| 字段 | 用途 |
| --- | --- |
| `returnId` | 本地或 OMS退货单号 |
| `omsReturnId` | OMS退货单唯一标识 |
| `orderId` | 关联订单号 |
| `userName` | 退货用户；用于列表和详情展示 |
| `company` | 上游退货单关联公司，仅作为业务关联数据保留，不在退货列表和详情中展示 |
| `items` | 退货SKU、数量、名称、规格和图片 |
| `reason`、`description` | 退货原因和买家说明 |
| `amount` | 涉及退款或抵扣金额 |
| `auditStatus` | 待人工审核、自动通过、人工通过或已拒绝 |
| `disposition` | 收货后处理方式 |
| `valueAddedServices` | 申请拆箱质检、申请换箱 |
| `reviewedBy`、`reviewedAt` | 审核人和审核时间 |
| `omsSyncStatus` | 审核指令提交 OMS 的同步结果 |
| `omsProgressStep` | 退货申请、运营审核、仓库收货和收货后处理的当前进度节点 |

## 10. 概览趋势计算

退货概览按当前管理权限内的全部退货单计算，不受退货单号、关联订单号或审核状态筛选条件影响。

百分比公式：`趋势百分比 =（当前值 - 对比值）/ 对比值 × 100%`。

| 指标 | 当前值 | 对比值 |
| --- | --- | --- |
| 待人工审核订单 | 当前时点审核状态为待人工审核的退货单数 | 7天前同一时点处于待人工审核状态的退货单数 |
| 待审核商品 | 当前待人工审核订单的商品件数之和 | 7天前同一时点待人工审核订单的商品件数之和 |
| 本月退货单 | 本月1日00:00至当前时点创建的退货单数 | 上一个自然月创建的退货单总数 |
| 本月退货额 | 本月1日00:00至当前时点退货单的退款或抵扣金额之和 | 上一个自然月退货单的退款或抵扣金额总和 |

- 待人工审核订单和待审核商品展示“vs 上周”；本月退货单和本月退货额展示“vs 上月”。
- 趋势保留一位小数。箭头表示数学增减，颜色表达业务好坏；待人工审核订单或退货额下降可使用正向颜色。
- 对比值为0且当前值为0时展示 `0.0%`；对比值为0且当前值大于0时展示“新增”。
- 当前页面的趋势百分比为静态原型值；正式环境由退货审核历史快照和退款金额流水计算。
