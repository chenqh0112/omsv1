# 独立站 - 退货管理页 PRD

## 页面的数据来源

- 当前本地原型从 `order-data.js` 读取订单和SKU明细，并据此生成退货单模拟数据；美国货盘V2提交的退货申请从浏览器本地存储 `oms-return-requests` 合并到列表。
- 收货、质检、上架、销毁和退款操作只更新当前页面内存数据；销毁费和库存回补以页面反馈模拟，不会真实写入费用或OMS库存。
- 正式环境应由售后服务提供退货单和处理记录，由订单服务提供原订单、用户与退款基准，由OMS同步收货、质检和回库结果，并由费用服务记录贴标、入库处理和销毁费用。

## 1. 页面定位

退货管理页用于管理对公订单及原出库单产生的退货，覆盖收货登记、质检、重新上架、销毁和退款。页面入口位于“管理端 > 售后 > 退货管理”。

## 2. 页面结构

页面包含退货概览、筛选区、退货单列表、分页区和处理弹窗。右侧注释默认展开并加载本 PRD。

## 3. 顶部统计

| 指标 | 统计口径 |
| --- | --- |
| 待处理退货 | 状态为待收货、已收货或质检中的退货单数量 |
| 退货商品 | 已收货或质检中退货单的商品件数合计 |
| 本月退货单 | 本月创建的退货单数量 |
| 本月退货额 | 本月退货单涉及的退款或抵扣金额合计 |

每个指标同时展示与上周或上月的趋势。

## 4. 筛选

- 退货单号和关联订单号支持部分匹配。
- 退货状态支持待收货、已收货、质检中、已上架、已销毁、待退款和已退款。
- 不同条件之间为“且”关系；搜索或重置后回到第一页。

## 5. 退货单列表

| 字段 | 说明 |
| --- | --- |
| 退货单号 | 系统生成的退货唯一编号，左对齐 |
| 关联订单号 | 对公采购订单或原出库单编号 |
| 退货用户/公司 | 提交退货的用户名和公司 |
| 退货SKU+数量 | 商品图片、名称、SKU种类和总件数 |
| 退货原因 | 质量问题、错发、买家拒收或多退 |
| 退货状态 | 当前退货处理节点 |
| 退货金额 | 当前退货涉及金额 |
| 操作 | 根据当前状态显示可执行动作 |

列表按每页8条真实分页，分页展示总条数、当前页和总页数。

## 6. 状态与操作

主流程为：`待收货 -> 已收货 -> 质检中 -> 已上架`。质检完成后，当前页面保留“上架”和“销毁”两个处理入口；运营人员根据已记录的质检结论选择后续动作。已销毁或待退款的退货单可继续执行退款并进入“已退款”。

| 操作 | 页面行为 |
| --- | --- |
| 收货 | 登记外包装完好或破损，状态更新为已收货 |
| 质检 | 记录可再售、需翻新贴标或只能销毁，状态更新为质检中 |
| 上架 | 商品回到 OMS 可用库存，可再次放入美国货盘，状态更新为已上架 |
| 销毁 | 填写销毁说明，状态更新为已销毁，并关联退货销毁费 |
| 退款 | 确认退款金额，状态更新为已退款 |

销毁操作要求填写处理说明；退款金额必须为大于或等于0的有效数字。收货、质检、上架、销毁和退款完成后，概览、列表和当前筛选结果立即重新计算。

## 7. 对公退货入口

美国货盘V2“我的订单”中，已发货订单显示“申请退货”。用户选择订单内商品、填写数量、原因和说明后提交，生成待收货退货单，并进入退货管理列表。

## 8. 概览趋势计算

退货概览按当前管理权限内的全部退货单计算，不受退货单号、关联订单号或退货状态筛选条件影响。

百分比公式：`趋势百分比 =（当前值 - 对比值）/ 对比值 × 100%`。

| 指标 | 当前值 | 对比值 |
| --- | --- | --- |
| 待处理退货 | 当前时点状态为待收货、已收货或质检中的退货单数 | 7天前同一时点处于相同状态范围的退货单数 |
| 退货商品 | 当前时点已收货或质检中退货单的商品件数之和 | 7天前同一时点对应状态退货商品件数之和 |
| 本月退货单 | 本月1日00:00至当前时点创建的退货单数 | 上一个自然月创建的退货单总数 |
| 本月退货额 | 本月1日00:00至当前时点退货单的退款或抵扣金额之和 | 上一个自然月退货单的退款或抵扣金额总和 |

- 待处理退货和退货商品展示“vs 上周”；本月退货单和本月退货额展示“vs 上月”。
- 趋势保留一位小数。箭头表示数学增减，颜色表达业务好坏；待处理退货或退货额下降可使用正向颜色。
- 对比值为0且当前值为0时展示 `0.0%`；对比值为0且当前值大于0时展示“新增”。
- 当前页面的趋势百分比为静态原型值，正式环境由退货单历史状态快照和退款金额流水计算。
