# 美国货盘PRD - 用户管理页 PRD

## 页面的数据来源

前端页面注册登录的用户。

## 1. 页面定位

用户管理页面向管理端系统管理员，用于维护对公采购用户资料、控制账号登录状态，并查看用户的订单和消费情况。

页面入口位于左侧导航“系统 > 用户管理”。

## 2. 页面结构

页面由以下区域组成：

1. 顶部栏：页面标题和身份切换入口。
2. 统计区：总用户数、活跃用户、本月新增、已冻结。
3. 筛选区：用户名或手机号、账号状态。
4. 用户列表：展示用户资料、活跃情况、订单和消费数据。
5. 账号状态确认弹窗：冻结或解冻用户。

## 3. 顶部统计

| 指标 | 统计口径 |
| --- | --- |
| 总用户数 | 平台中已创建的全部用户数量 |
| 活跃用户 | 最近30天内有登录记录的用户数量 |
| 本月新增 | 注册时间在当前自然月内的用户数量 |
| 已冻结 | 账号状态为“冻结”的用户数量 |

每张统计卡片左上角同排展示放大后的指标标题和小字号口径说明，主体数字使用大字号突出。趋势箭头、变化比例和对比周期位于数字同一行右侧；总用户、活跃用户和本月新增展示增长趋势，已冻结展示下降趋势。内容区域较窄时保留趋势方向和百分比，隐藏较长的对比周期。

创建用户或变更账号状态后，统计数据立即重新计算。

## 4. 筛选区

| 筛选项 | 控件 | 匹配规则 |
| --- | --- | --- |
| 用户名/手机号 | 文本输入框 | 同时匹配用户名和手机号，支持模糊查询 |
| 账号状态 | 下拉选择 | 全部、正常、冻结 |

交互规则：

- 多个筛选条件之间为“且”关系。
- 点击“搜索”或在筛选输入框按 Enter 后刷新列表并回到第一页。
- 点击“重置”清空全部筛选条件并回到第一页。

## 5. 用户列表

| 字段 | 展示内容 |
| --- | --- |
| 用户名 | 用户头像缩写和登录用户名 |
| 联系方式 | 邮箱和电话，两行展示 |
| 注册时间 | 用户首次创建日期 |
| 最近登录 | 最近一次成功登录时间；从未登录时显示“尚未登录” |
| 账号状态 | 正常或冻结，使用不同颜色标识 |
| 关联订单数 | 该用户名提交的订单总数 |
| 累计消费 | 该用户全部非取消订单的金额合计 |
| 操作 | 冻结或解冻、查看订单 |

用户名列的表头和内容均左对齐。手机号在联系方式列展示并支持关键词匹配；公司和用户类型仅保留为创建和编辑资料，不在用户列表中展示，也不参与筛选。

列表每页展示10条用户。分页区展示结果总数、上一页、当前页/总页数和下一页。


## 6. 冻结与解冻

### 6.1 冻结

- 正常用户展示“冻结”操作。
- 点击后打开二次确认弹窗。
- 弹窗说明冻结后用户不能登录和下单，已有订单不会删除。
- 确认后账号状态变为“冻结”，顶部已冻结数量加1。

### 6.2 解冻

- 冻结用户展示“解冻”操作。
- 点击后打开二次确认弹窗。
- 确认后账号状态恢复为“正常”，用户可以重新登录和下单。
- 顶部已冻结数量同步减少。

## 7. 查看订单

- 点击“查看订单”跳转到订单管理页。
- 跳转 URL 携带当前用户唯一 ID。
- 订单管理页按用户唯一 ID 执行精确查询，不使用用户名或公司匹配。
- 订单管理中的履约状态由 OMS 同步；用户管理页只读取关联订单数与非已关闭订单消费额，不在本页修改订单状态。

## 8. 数据关联

用户、订单、收藏和地址通过不可变的用户唯一 ID 关联：

- 关联订单数：匹配当前用户 ID 的全部订单数量。
- 累计消费：匹配当前用户 ID 且状态不为“已关闭”的订单金额合计。
- 用户名仅用于展示，修改用户名不得影响历史订单、收藏、地址和统计。

## 9. 账号状态规则

| 状态 | 登录 | 下单 | 管理端操作 |
| --- | --- | --- | --- |
| 正常 | 允许 | 允许 | 冻结、查看订单 |
| 冻结 | 禁止 | 禁止 | 解冻、查看订单 |

冻结账号不会删除用户资料、历史订单或累计消费记录。

冻结后必须立即使该用户现有公开商城会话失效；用户未提交的购物车可以保留为本地草稿，但不占用任何库存，也不能继续结算或下单。

## 10. 页面反馈

- 创建、编辑、冻结和解冻结果统一使用顶部 Toast。
- 必填信息缺失时使用警告 Toast。
- 创建、编辑和状态确认弹窗可通过关闭按钮、点击遮罩或按 Escape 关闭。

## 13. 概览趋势计算

用户概览使用当前管理权限内的全部用户数据，不受用户名/手机号或账号状态筛选条件影响。

百分比公式：`趋势百分比 =（当前值 - 对比值）/ 对比值 × 100%`。

| 指标 | 当前值 | 对比值 |
| --- | --- | --- |
| 总用户数 | 当前时点已注册且未删除的用户数 | 上一个自然月最后一日结束时的用户数 |
| 活跃用户 | 当前日期向前30天内至少成功登录1次的去重用户数 | 紧邻当前窗口之前30天内至少成功登录1次的去重用户数 |
| 本月新增 | 本月1日00:00至当前时点新注册的用户数 | 上月1日00:00至与当前月相同日、相同时间的新注册用户数；若上月无对应日期，取上月最后一天 |
| 已冻结 | 当前时点账号状态为冻结的用户数 | 上一个自然月最后一日结束时被冻结的用户数 |

- 总用户数、活跃用户和已冻结展示“vs 上月”，本月新增展示“vs 上月同期”。
- 趋势保留一位小数。箭头表示数学增减；已冻结下降属于正向表现，可使用正向颜色。
- 对比值为0且当前值为0时展示 `0.0%`；对比值为0且当前值大于0时展示“新增”。
- 当前页面的 `9.2%`、`6.8%`、`18.5%`、`4.1%` 为原型示例值，正式环境由用户注册、登录和账号状态历史数据计算。

