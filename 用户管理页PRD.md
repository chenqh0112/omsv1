# 独立站 - 用户管理页 PRD

## 1. 页面定位

用户管理页面向管理端系统管理员，用于维护对公采购用户资料、控制账号登录状态，并查看用户的订单和消费情况。

页面入口位于左侧导航“管理端 > 系统 > 用户管理”。

## 2. 页面结构

页面由以下区域组成：

1. 顶部栏：页面标题、手动创建用户和身份切换入口。
2. 统计区：总用户数、活跃用户、本月新增、已冻结。
3. 筛选区：用户名或公司、用户类型、账号状态。
4. 用户列表：展示用户资料、活跃情况、订单和消费数据。
5. 用户表单弹窗：创建或编辑用户。
6. 账号状态确认弹窗：冻结或解冻用户。

## 3. 顶部统计

| 指标 | 统计口径 |
| --- | --- |
| 总用户数 | 平台中已创建的全部用户数量 |
| 活跃用户 | 最近30天内有登录记录的用户数量 |
| 本月新增 | 注册时间在当前自然月内的用户数量 |
| 已冻结 | 账号状态为“冻结”的用户数量 |

每张统计卡片左上角同排展示放大后的指标标题和小字号口径说明，主体数字使用大字号突出。趋势箭头、变化比例和对比周期位于数字同一行右侧；总用户、活跃用户和本月新增展示增长趋势，已冻结展示下降趋势。内容区域较窄时保留趋势方向和百分比，隐藏较长的对比周期。

创建用户或变更账号状态后，统计数据立即重新计算。

## 4. 筛选区

| 筛选项 | 控件 | 匹配规则 |
| --- | --- | --- |
| 用户名/公司 | 文本输入框 | 同时匹配用户名和公司名称，支持部分匹配 |
| 用户类型 | 下拉选择 | 全部、B2B分销商、外部客户 |
| 账号状态 | 下拉选择 | 全部、正常、冻结 |

交互规则：

- 多个筛选条件之间为“且”关系。
- 点击“搜索”或在筛选输入框按 Enter 后刷新列表并回到第一页。
- 点击“重置”清空全部筛选条件并回到第一页。

## 5. 用户列表

| 字段 | 展示内容 |
| --- | --- |
| 用户名 | 用户头像缩写和登录用户名 |
| 联系方式 | 邮箱和电话，两行展示 |
| 注册时间 | 用户首次创建日期 |
| 最近登录 | 最近一次成功登录时间；从未登录时显示“尚未登录” |
| 账号状态 | 正常或冻结，使用不同颜色标识 |
| 关联订单数 | 该用户名提交的订单总数 |
| 累计消费 | 该用户全部非取消订单的金额合计 |
| 操作 | 编辑、冻结或解冻、查看订单 |

用户名列的表头和内容均左对齐。公司和用户类型保留为筛选及编辑资料，但不在用户列表中展示。

列表每页展示8条用户。分页区展示结果总数、上一页、当前页/总页数和下一页。

## 6. 手动创建用户

点击页面顶部“手动创建用户”打开创建弹窗。

| 字段 | 控件与规则 |
| --- | --- |
| 用户名 | 文本输入框，必填 |
| 公司 | 文本输入框，必填 |
| 用户类型 | 下拉选择，默认B2B分销商 |
| 账号状态 | 下拉选择，默认正常 |
| 邮箱 | 邮箱输入框，必填 |
| 电话 | 文本输入框，必填 |

提交成功后：

- 新用户插入列表首位。
- 注册时间记录为当前日期。
- 最近登录显示“尚未登录”。
- 关联订单数和累计消费初始化为0。
- 顶部总用户数和本月新增同步更新。
- 显示顶部 Toast“用户创建成功”。

## 7. 编辑用户

- 点击“编辑”后，弹窗回填当前用户信息。
- 支持修改用户名、公司、用户类型、账号状态、邮箱和电话。
- 必填字段未填写完整时不提交，并显示警告 Toast。
- 保存成功后刷新列表和顶部统计，并显示 Toast“用户信息已更新”。

## 8. 冻结与解冻

### 8.1 冻结

- 正常用户展示“冻结”操作。
- 点击后打开二次确认弹窗。
- 弹窗说明冻结后用户不能登录和下单，已有订单不会删除。
- 确认后账号状态变为“冻结”，顶部已冻结数量加1。

### 8.2 解冻

- 冻结用户展示“解冻”操作。
- 点击后打开二次确认弹窗。
- 确认后账号状态恢复为“正常”，用户可以重新登录和下单。
- 顶部已冻结数量同步减少。

## 9. 查看订单

- 点击“查看订单”跳转到订单管理页。
- 跳转 URL 携带当前用户名。
- 订单管理页自动填入下单用户筛选条件并执行搜索。
- 结果仅展示用户名或公司匹配的订单。

## 10. 数据关联

用户的订单数据通过用户名与订单记录关联：

- 关联订单数：匹配当前用户名的全部订单数量。
- 累计消费：匹配当前用户名且状态不为“已取消”的订单金额合计。
- 公司信息以用户资料为准，订单保留用户下单时的公司快照。

## 11. 账号状态规则

| 状态 | 登录 | 下单 | 管理端操作 |
| --- | --- | --- | --- |
| 正常 | 允许 | 允许 | 编辑、冻结、查看订单 |
| 冻结 | 禁止 | 禁止 | 编辑、解冻、查看订单 |

冻结账号不会删除用户资料、历史订单或累计消费记录。

## 12. 页面反馈

- 创建、编辑、冻结和解冻结果统一使用顶部 Toast。
- 必填信息缺失时使用警告 Toast。
- 创建、编辑和状态确认弹窗可通过关闭按钮、点击遮罩或按 Escape 关闭。
- 黄色“查看注释”按钮默认显示，支持拖动和记忆位置；注释面板默认打开且宽度可调。
