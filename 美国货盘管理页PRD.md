# 独立站 - 美国货盘管理页 PRD

## 1. 页面定位

美国货盘管理页用于内部运营人员维护对公美国货盘。页面连接 OMS 可用库存与对公销售库存，支持把 SKU 放入美国货盘、调整对公展示数量、控制外部可见性、查看预占情况以及取消对公。

页面入口位于左侧导航“管理端 > 库存 > 美国货盘管理”。

## 2. 使用角色

页面面向内部管理人员。当前原型支持通过身份头像切换以下身份：

| 身份 | 数据范围 |
| --- | --- |
| 超管 | 查看和管理全部公司、业务组的货盘数据 |
| 公司A管理员 | 查看和管理公司A范围内的数据 |
| 公司A业务组A管理员 | 查看和管理公司A业务组A范围内的数据 |
| 公司A业务组A业务员 | 查看本人权限范围内的数据 |

正式环境的数据范围必须以后端鉴权结果为准，前端不得通过隐藏控件代替权限校验。

## 3. 页面结构

页面由以下区域组成：

1. 顶部栏：页面标题、当前身份头像和身份切换入口。
2. 统计区：货盘SKU数、货盘总量、可售数量、已预占。
3. 筛选区：SKU编码、SKU名称、搜索和重置。
4. 库存工作区：美国货盘、未放到美国货盘两个 Tab。
5. 操作弹窗：单个放入、调整数量和批量放入确认。
6. 页面注释：默认展开，支持收起、拖动入口和调整注释面板宽度。

## 4. 顶部统计

| 指标 | 计算口径 | 趋势示例 |
| --- | --- | --- |
| 货盘SKU数 | 当前权限范围内，已经放入美国货盘的 SKU 数量 | ↑6.5% vs 上周 |
| 货盘总量 | 已放入美国货盘的对公展示数量之和 | ↑9.8% vs 上周 |
| 可售数量 | 所有货盘 SKU 的“对公展示数量 - 已预占”之和 | ↑7.2% vs 上周 |
| 已预占 | 外部用户已下单但尚未发货，占用的件数之和 | ↑12.4% vs 上周 |

每张统计卡片左上角同排展示放大后的指标标题和小字号口径说明，主体数字使用大字号与指标色突出。趋势位于数字同一行右侧，方便同时查看当前值和周变化。

统计区基于当前身份权限范围计算，不随 SKU编码或 SKU名称搜索条件变化。

## 5. 筛选区

| 筛选项 | 控件 | 匹配规则 |
| --- | --- | --- |
| SKU编码 | 文本输入框 | 支持部分匹配，不区分大小写 |
| SKU名称 | 文本输入框 | 支持部分匹配，不区分大小写 |

筛选规则：

- 两个筛选条件之间为“且”关系。
- 点击“搜索”后，当前 Tab 列表和结果条数同步更新。
- 在筛选输入框内按 Enter，执行搜索。
- 点击“重置”清空两个条件，恢复当前身份下的全部数据。
- 筛选条件在两个 Tab 间共用，切换 Tab 时保留当前条件。

点击“导出”下载当前 Tab、当前筛选条件和当前排序下的全部结果，不仅导出当前分页。导出文件为 CSV，完成后显示 Toast“导出成功”。

## 6. Tab 与数据关系

| Tab | 数据范围 | 主要操作 |
| --- | --- | --- |
| 美国货盘 | 已经放入对公美国货盘的 SKU | 调整数量、取消对公、切换对外可见 |
| 未放到美国货盘 | OMS 中存在但尚未进入对公美国货盘的 SKU | 单个放入、勾选后批量放入 |

Tab 名称右侧展示对应数据总数。筛选后的列表底部展示当前筛选结果条数。

## 7. 美国货盘列表

### 7.1 字段顺序

| 字段 | 定义 |
| --- | --- |
| SKU | 商品图片、SKU名称、规格和SKU编码的组合信息 |
| 库龄 | 默认展示该 SKU 当前最大库龄；悬停或聚焦后展示每次入库的库龄、上架日期和统计日期；支持排序 |
| 对公展示数量 | 当前设置在对公美国货盘中展示的商品件数 |
| 可售数量 | 对公展示数量减去已预占数量后，可继续销售的件数；支持排序 |
| 已预占 | 外部用户已下单但尚未发货，占用的件数 |
| 预占率 | 已预占数量除以对公展示数量，保留一位小数；支持排序 |
| 对外可见 | 控制外部用户是否能在对公货盘页面看到该 SKU |
| 所属卖家 | 放入该批货的内部团队或外部卖家 |
| 辅助决策建议 | 根据库龄、预占率和剩余可售数量展示库存健康、建议补货或建议清滞销 |
| 操作 | 调整数量、取消对公 |

“对公展示数量、可售数量、已预占、预占率”表头右侧显示问号图标。鼠标移入或键盘聚焦时，使用与库存管理概览问号一致的深色浮层显示字段定义。

库龄、可售数量和预占率表头支持排序。首次点击默认降序，再次点击切换为升序；当前排序方向通过箭头显示。

### 7.2 SKU 信息

- 点击 SKU 图片打开大图预览。
- SKU名称作为主要识别信息。
- 规格和SKU编码显示在名称下方。
- SKU编码使用等宽字体，便于核对。

### 7.3 对外可见

- 开关开启时，外部用户可以看到该 SKU。
- 开关关闭时，该 SKU 仍保留在内部美国货盘中，但不在对公页面展示。
- 切换成功后显示全局 Toast“已对外展示”或“已对外隐藏”。

### 7.4 调整数量

点击“调整数量”打开数量弹窗：

| 字段 | 规则 |
| --- | --- |
| 选择SKU | 固定为当前行 SKU，不允许切换 |
| 可用库存 | 展示 OMS 可用库存，作为输入上限 |
| 对公展示数量 | 必须大于 0，不得超过 OMS 可用库存，不得小于已预占 |
| 对外可见 | 可同步调整当前 SKU 的外部展示状态 |

保存成功后，货盘总量、可售数量和当前行数据同步更新，并显示 Toast“数量调整成功”。

### 7.5 取消对公

- 已预占数量为 0 时，点击“取消对公”将 SKU 移入“未放到美国货盘”。
- 已预占数量大于 0 时不允许取消，显示 Toast“该SKU存在已预占数量，暂不能取消对公”。
- 取消成功后清空对公展示数量并关闭对外可见，显示 Toast“已取消对公”。

## 8. 未放到美国货盘列表

| 字段 | 定义 |
| --- | --- |
| 选择 | 勾选当前 SKU，用于批量放入 |
| SKU | 商品图片、SKU名称、规格和SKU编码 |
| 库龄 | 默认展示该 SKU 当前最大库龄；悬停或聚焦后展示每次入库的库龄、上架日期和统计日期 |
| OMS可用库存 | 仓库当前实际可用于放入美国货盘的数量 |
| 辅助决策建议 | 库龄较长时提示建议清滞销，库存充足且库龄健康时提示建议放货，其余提示持续观察 |
| 操作 | “放到美国货盘”按钮 |

OMS可用库存为 0 的 SKU：

- 行选择框不可用。
- “放到美国货盘”按钮不可用。
- 不允许进入单个或批量放入流程。

表头选择框只选择当前页中 OMS可用库存大于 0 的 SKU，并支持全选、取消全选和半选状态。跨页勾选结果保留，可统一批量放入。

两个 Tab 分别维护当前页，每页展示8条数据。分页显示结果总数、当前页和总页数；切换 Tab 保留各自页码，搜索后两个 Tab 均回到第一页。

## 9. 单个放入美国货盘

点击行内“放到美国货盘”打开弹窗：

| 字段 | 规则 |
| --- | --- |
| 选择SKU | 默认选择当前行，也可切换到其他未放入 SKU |
| 可用库存 | 展示 OMS 可用库存和数量上限 |
| 对公展示数量 | 必须大于 0，且不得超过 OMS 可用库存 |
| 对外可见 | 默认开启 |

确认后：

- SKU 从“未放到美国货盘”移入“美国货盘”。
- 已预占初始化为 0。
- 统计区和两个 Tab 数量同步更新。
- 自动切换到“美国货盘”Tab。
- 显示 Toast“已放入美国货盘”。

## 10. 批量放入美国货盘

### 10.1 批量入口

- “批量放入美国货盘”按钮只在“未放到美国货盘”Tab 显示。
- 未勾选 SKU 时按钮不可用。
- 勾选一个或多个可用 SKU 后按钮可用。

### 10.2 二次确认弹窗

弹窗使用表格展示全部已选 SKU：

| 字段 | 规则 |
| --- | --- |
| SKU | 展示图片、名称、规格和编码 |
| OMS可用库存 | 展示当前库存上限 |
| 对公展示数量 | 可逐行编辑，默认等于当前 OMS 可用库存 |

任意一行对公展示数量小于 1 或超过 OMS可用库存时，不允许确认，并显示数量提示 Toast。

确认成功后：

- 所选 SKU 全部进入“美国货盘”。
- 每个 SKU 的已预占初始化为 0。
- 每个 SKU 默认对外可见。
- 清空已选状态。
- 自动切换到“美国货盘”Tab。
- 刷新统计区、Tab 数量和列表结果。
- 显示 Toast“已批量放入美国货盘”。

## 11. 页面状态与反馈

- 当前筛选无结果时，列表显示空状态，结果条数为 0。
- 图片预览、单个操作弹窗和批量确认弹窗支持点击遮罩或按 Escape 关闭。
- 所有操作结果统一使用顶部 Toast，不使用浏览器原生提示框。
- 页面刷新后重新从数据源加载货盘状态，前端临时选择状态不保留。
- “导出”按钮在“美国货盘”和“未放到美国货盘”两个 Tab 均显示，导出当前 Tab 的当前筛选结果为 CSV；导出成功显示 Toast“导出成功”。

## 12. 数据字段

| 字段 | 用途 |
| --- | --- |
| `sku` | SKU编码与数据唯一标识 |
| `name` | SKU名称 |
| `spec` | SKU规格 |
| `img` | SKU图片 |
| `ageRecords` | 多次入库的库龄记录，包含库龄天数、上架日期和统计日期 |
| `omsAvailable` | OMS可用库存 |
| `marked` | 是否已进入美国货盘 |
| `markedQty` | 对公展示数量 |
| `reserved` | 已预占数量 |
| `visible` | 是否对外可见 |
| `seller` | 所属卖家或内部团队 |

可售数量由页面根据 `markedQty - reserved` 计算，结果不得小于 0。
