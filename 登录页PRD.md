# 独立站 - 登录页 PRD

## 1. 页面定位

登录页是管理端入口，用于展示账号登录表单并进入库存管理。页面入口位于左侧导航“管理端 > 登录”。

## 2. 页面结构

页面包含顶部栏、登录面板和页面注释。登录面板居中展示标题、说明、账号、密码、记住密码、滑块验证和登录按钮。

## 3. 登录表单

| 字段 | 控件 | 当前行为 |
| --- | --- | --- |
| 账号 | 文本输入框 | 支持浏览器用户名自动填充 |
| 密码 | 密码输入框 | 支持浏览器当前密码自动填充 |
| 记住密码 | 复选框 | 原型仅展示选择状态 |
| 滑块验证 | 0至100范围滑块 | 拖到100%时显示“验证完成” |
| 登录 | 主按钮 | 点击后进入库存管理页 |

当前原型不校验账号、密码、记住密码或滑块是否完成。提交表单后直接跳转至 `index.html`。

登录跳转不会创建真实会话，也不会改变右上角当前模拟身份；账号记忆和鉴权均仅作为后续接入点。

## 4. 滑块交互

- 初始值为0，状态文案为“拖动滑块完成验证”。
- 拖动过程中向辅助技术同步当前百分比。
- 值达到100时，状态文案和可访问说明均更新为“验证完成”。
- 滑块未完成不阻止登录。

## 5. 全局能力

- 右上角头像可切换原型身份。
- 黄色“查看注释”按钮默认显示，支持拖动。
- 注释区域默认展开，可收起并调整宽度。
