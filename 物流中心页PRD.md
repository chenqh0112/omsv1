# 该页面只修改头程-批次号弹窗里上传箱标的逻辑
## 并且只针对海外仓美国货盘特殊处理


## 上传箱标入口

### 入口规则 （沿用之前的逻辑）

- 只有当前批次状态为“包装完成”时，基础信息区域才展示箱标“上传”按钮。
- 点击“上传”后打开“上传箱标”弹窗，并保留当前头程批次号作为本次上传的关联上下文。
- 上传弹窗打开后，当前头程弹窗保持在当前批次，不允许将箱标关联到其他批次。

## 上传箱标弹窗

### 上传箱标流程

点击“上传箱标文件”后按以下流程处理：

1. 校验当前表单必填项，校验失败时停止提交并提示对应字段错误。
2. 判断当前地址组合是否为“海外仓&美国&美国货盘海外仓”。
3. 如果不是目标地址组合，沿用原有箱标上传逻辑处理，不改变原有分支行为。
4. 如果是目标地址组合，调用海外仓配货入库单接口 `/oms/api/warehouse/inbound_form`。
5. 接口异常时展示错误信息，不关闭上传弹窗，不建立批次关联，不展示成功提示；具体错误文案沿用接口返回或原有错误处理规则。
6. 接口创建成功后，获取本次生成或上传的箱标信息。
7. 将箱标信息与当前头程批次号关联，确保箱标归属当前批次。
8. 关联成功后提示 `上传箱标文件成功`。
9. 关闭上传箱标弹窗，并在头程弹窗的“箱标”文本下方回显箱标文件信息，沿用原有回显逻辑。
10. 上传成功后的头程状态是否由接口自动推进为“已传箱标”，需与现有状态流转保持一致；状态写入归属待确认，前端不应重复推进造成状态不一致。

## 接口及数据关联

| 接口/数据 | 用途 | 规则 |
| --- | --- | --- |
| `/oms/api/warehouse/inbound_form` | 为目标仓库组合创建海外仓配货入库单 | 仅对“海外仓&美国&美国货盘海外仓”分支调用；异常时终止后续流程 |
| 箱标信息 | 获取本次上传成功后的箱标文件或箱标编码信息 | 必须与当前头程批次号建立关联后，才允许提示成功并回显 |
| 头程批次号 | 标识本次箱标上传归属的批次 | 从进入头程弹窗的当前批次上下文取得，不允许由用户重新选择 |

接口请求字段、响应字段、幂等标识和箱标信息的具体结构待后端接口定义确认。

## 上传结果及异常处理

- 必填字段为空：对应字段标红并提示不能为空，阻止提交。
- 目标仓库组合识别失败：不得误调用海外仓配货入库单接口，按原有逻辑或错误处理规则处理；具体提示待确认。
- 海外仓配货入库单接口异常：保留用户已填写内容，允许修正后重试，不产生成功关联。
- 箱标信息获取失败或批次关联失败：不关闭弹窗，不展示“上传箱标文件成功”，并提示失败原因。
- 用户点击“关闭”、右上角关闭或遮罩关闭：取消本次未提交操作，当前表单内容是否保留待确认。
- 重复点击提交：应避免同一批次重复创建入库单或重复关联箱标，幂等和重复上传规则待确认。

## 页面刷新及回显

- 上传成功后关闭上传箱标弹窗，保留头程弹窗打开。
- 箱标信息回显在头程弹窗基础信息区域的“箱标”文本下方。
- 页面主表格中的箱标编码、批次状态等字段应与最新服务端数据保持一致；是否立即刷新整行数据待确认。
- 关闭头程弹窗后再次打开，应读取并展示已关联的箱标信息，不依赖当前浏览器临时状态。


