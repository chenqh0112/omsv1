# 独立站 - 订单管理页 PRD

## 1. 页面定位

订单管理页面向管理端运营人员，用于查看对公订单、识别需要人工审核的异常订单，并追踪 OMS 回传的履约状态。页面入口位于“管理端 > 订单 > 订单管理”。

对公页面提交订单时通过同一接口直接在 OMS 下单。正常订单不需要人工确认或标记发货，提交成功后进入“待分配仓库”；只有命中审核规则的订单进入“待审核”。

## 2. 页面结构

页面包含顶部统计、订单筛选、订单列表、审核弹窗和订单详情。右侧注释默认展开并加载本 PRD。

## 3. 顶部统计

| 指标 | 统计口径 |
| --- | --- |
| 待审核订单 | 触发审核规则、等待人工处理的订单数量 |
| 处理中 | 待分配仓库、待仓库拣货、待物流收揽的订单数量 |
| 已发货 | OMS 回传状态为已发货的订单数量 |
| 本月成交额 | 本月除已关闭外的订单金额合计 |

每张卡片显示当前值及趋势。订单审核后统计立即重新计算；其他履约状态以 OMS 最新同步结果为准。

## 4. 待审核规则

订单命中任一规则即进入“待审核”：

1. 单笔订单金额超过 5000 美元。
2. 新用户首次下单。
3. 本次下单总数量超过该用户历史订单平均数量的 3 倍。
4. OMS 库存锁定失败。

订单记录需要保存命中的全部规则。列表点击审核或查看详情时展示规则，不只展示“待审核”状态。

## 5. 筛选

- 订单号支持部分匹配。
- 下单用户/公司同时匹配用户名和公司名称。
- 状态可选：待审核、待分配仓库、待仓库拣货、待物流收揽、已发货、商品缺货、已关闭、拦截发货成功。
- 多个条件之间为“且”关系；搜索和重置后回到第一页。
- 从用户管理点击“查看订单”进入时，页面读取 URL 中的用户名参数，自动填入“下单用户/公司”并执行筛选。

## 6. 订单列表

| 字段 | 展示内容 |
| --- | --- |
| 订单号 | 系统订单编号，左对齐 |
| 下单用户 | 用户名和公司，两行展示 |
| 下单时间 | 对公订单提交时间 |
| SKU明细 | 首个SKU名称、SKU种类数和商品总件数 |
| 订单金额 | 货款、操作费和物流费合计 |
| 订单状态 | OMS 最新状态，不同状态使用不同颜色 |
| 物流单号 | OMS 已生成时展示，否则显示“-” |
| 操作 | 待审核订单显示审核通过、关闭订单和查看详情；其他订单只显示查看详情 |

列表每页8条，分页展示总条数、当前页和总页数。

## 7. 订单状态

| 状态 | 含义 | 状态来源 |
| --- | --- | --- |
| 待审核 | 订单触发审核规则，暂停进入正常履约 | 风控规则 |
| 待分配仓库 | 订单已同步 OMS，等待选择履约仓库 | OMS |
| 待仓库拣货 | 已分配仓库，等待仓库拣货打包 | OMS |
| 待物流收揽 | 包裹已准备，等待物流商揽收 | OMS |
| 已发货 | 物流商已揽收并进入运输 | OMS |
| 商品缺货 | OMS 无法锁定或分配足够库存 | OMS |
| 已关闭 | 订单已终止，不再履约 | 审核操作或 OMS |
| 拦截发货成功 | 发货前拦截成功，包裹不再出库 | OMS |

正常主路径为：`待分配仓库 -> 待仓库拣货 -> 待物流收揽 -> 已发货`。

## 8. 审核操作

只有“待审核”订单提供人工操作：

| 操作 | 页面行为 |
| --- | --- |
| 审核通过 | 展示触发规则；确认后状态变为待分配仓库，继续由 OMS 履约 |
| 关闭订单 | 展示触发规则并要求填写关闭原因；确认后状态变为已关闭 |

普通订单不显示确认或拒绝操作。任何订单都不显示“标记已发货”，发货状态和物流单号由 OMS 接口同步。

## 9. 订单详情

详情展示：

- 下单用户、公司、时间、状态、物流方式、物流单号和收货信息。
- 待审核订单命中的全部审核规则。
- SKU图片、名称、规格、编码、单价、数量和小计。
- 商品金额、操作费、物流费和订单金额。
- 订单已提交并同步 OMS、订单校验、分配仓库、仓库拣货、物流收揽和已发货时间线。
- 商品缺货、已关闭、拦截发货成功以异常节点显示。

## 10. 数据与反馈

- 对公下单接口返回订单编号、状态、库存锁定结果和审核规则。
- OMS 状态变化后通过接口同步订单状态、仓库和物流单号。
- 页面操作结果统一使用全局 Toast。
- 审核通过或关闭后立即刷新列表、分页和统计。
- 空结果时列表展示空状态，总条数为0，分页保持为 `1 / 1`。
