# 独立站 - 订单管理页 PRD

## 1. 页面定位

订单管理页面向管理端运营人员，用于查看前端用户下的订单、识别需要人工审核的异常订单。

页面入口位于“订单 > 订单管理”。


## 2. 数据来源
用户在对公页面提交订单时，本系统先创建本地订单，并保存下单用户、收货信息、SKU 明细、金额、审核结果和创建时间。

系统执行订单审核规则（订单命中任一规则即进入“待审核”）：
1. 单笔订单金额超过 5000 美元。
2. 新用户首次下单，使用用户唯一 ID 判断。
3. 本次下单总数量超过该用户历史订单平均数量的 3 倍，平均数量仅统计历史未关闭订单，无历史订单时不计算本条规则。
4. OMS 库存锁定失败。

订单记录需要保存命中的全部规则。列表点击审核或查看详情时展示规则，不只展示“待审核”状态。

接着：
- 未命中审核规则：调用接口创建 OMS 订单，OMS 创建成功后订单进入“待分配仓库”。
- 命中本地审核规则：订单状态为“待审核”，不调用接口创建 OMS 订单。
- OMS 返回库存锁定失败：订单进入“待审核”，并记录“OMS 库存锁定失败”规则。(?)

本系统保存 OMS 订单编号、包裹编号和本地订单编号的对应关系。

系统定时调用 OMS 状态查询接口，拉取订单或包裹的最新状态，更新本地订单展示信息。

### oms接口如下：

- 创建包裹明细：/oms/api/indent/indent_parcel/add
- 批量创建包裹明细：/oms/api/indent/indent_parcel/add_batch
- 批量补传包裹面单：/oms/api/indent/indent_parcel/supplement_label_batch
- 批量修改包裹备注：/oms/api/indent/indent_parcel/update_remark_batch
- 按包裹编号批量查询包裹状态：/oms/api/indent/indent_parcel/status_list_by_parcel_code
- 按订单编号批量查询包裹状态：/oms/api/indent/indent_parcel/status_list_by_indent_code

## 3. 页面结构

页面包含顶部统计、订单筛选、订单列表、审核弹窗和订单详情。

## 4. 顶部统计

| 指标 | 统计口径 |
| --- | --- |
| 待审核订单 | 触发审核规则、等待人工处理的订单数量 |
| 处理中 | 待分配仓库、待仓库拣货、待物流收揽的订单数量 |
| 已发货 | OMS 回传状态为已发货的订单数量 |
| 本月成交额 | 本月除已关闭外的，只统计审核通过且未关闭的订单金额合计 |

每张卡片显示当前值及趋势。订单审核后统计立即重新计算；其他履约状态以 OMS 最新同步结果为准。


## 5. 筛选

- 下单用户/手机号/订单号支持模糊查询。
- 订单状态可选：待审核、待分配仓库、待仓库拣货、待物流收揽、已发货、商品缺货、已关闭、拦截发货成功。
- 多个条件之间为“且”关系；搜索和重置后回到第一页。
- 从"用户管理"点击“查看订单”进入本页面时，页面读取 URL 中的用户名参数，自动填入“下单用户/手机号”并执行筛选。

## 6. 订单列表

| 字段 | 展示内容 | 数据来源 |
| --- | --- | --- |
| 订单号 | 系统订单编号，左对齐 | OMS接口获取 |
| 下单用户 | 用户名和手机号，两行展示 | 前端用户 |
| 下单时间 | 对公订单提交时间 | 前端订单提交随时间 |
| SKU明细 | 首个SKU名称、SKU种类数和商品总件数 | 前端用户所选SKU信息 |
| 订单金额 | 货款、操作费和物流费合计（订单统一使用 USD） | 用户所选的SKU金额相加 |
| 订单状态 | OMS 最新状态，不同状态使用不同颜色 | 状态写死，详细见8 |
| 物流单号 | OMS 已生成时展示，否则显示“-” | ==要考虑下是本系统生成还是OMS生成== |
| 操作 | 待审核订单显示审核通过、关闭订单和查看详情；其他订单只显示查看详情 | - |

列表每页10条，分页展示总条数、当前页和总页数。

## 7. 订单状态

| 状态 | 含义 | 状态来源 |
| --- | --- | --- |
| 待审核 | 订单触发审核规则，暂停进入正常流程 | 风控规则 |
| 待分配仓库 | 订单已同步 OMS，等待仓库作业 | OMS |
| 待仓库拣货 | 已分配仓库，等待仓库拣货打包 | OMS |
| 待物流收揽 | 包裹已准备，等待物流商揽收 | OMS |
| 已发货 | 物流商已揽收并进入运输 | OMS |
| 商品缺货 | OMS 无法锁定或分配足够库存 | OMS |
| 已关闭 | 订单已终止 | 审核操作或 OMS |
| 拦截发货成功 | 发货前拦截成功，包裹不再出库 | OMS |

正常主路径为：`待分配仓库 -> 待仓库拣货 -> 待物流收揽 -> 已发货`。

## 8. 审核操作

只有“待审核”订单提供人工操作：

| 操作 | 页面行为 |
| --- | --- |
| 审核通过 | 展示触发规则；审核通过后先重新发起库存锁定；锁定成功才进入待分配仓库，仍失败则进入商品缺货。 |
| 关闭订单 | 展示触发规则并要求填写关闭原因；确认后状态变为已关闭；由于“待审核”状态本身就是在本系统里控制的，所以该操作无需通知OMS。 |

普通订单不显示确认或拒绝操作。发货状态和物流单号由 OMS 接口同步==（物流要考虑下是本系统生成还是OMS生成）==。

## 9. 订单详情

详情展示：

- 下单用户、手机号、时间、状态、物流方式、物流单号和收货信息。
- 待审核订单命中的全部审核规则。
- SKU图片、名称、规格、编码、单价、数量和小计。
- 商品金额、操作费、物流费和订单金额。
- 订单已提交并同步 OMS、订单校验、分配仓库、仓库拣货、物流收揽和已发货时间线。
- 商品缺货、已关闭、拦截发货成功以异常节点显示。

## 10. 数据与反馈

- 本系统定时调用 OMS 状态查询接口，拉取订单状态、仓库、包裹号和物流单号，并更新本地订单
- 审核通过或关闭后立即刷新列表、分页和统计。
- 空结果时列表展示空状态，总条数为0，分页保持为 `1 / 1`。

## 11. 原型全局能力（与开发无关，只是辅助理解）

- 右上角头像可切换原型身份。
- 黄色“查看注释”按钮默认显示，支持拖动。
- 注释区域默认展开，可收起并调整宽度。

## 12. 概览趋势计算

订单概览使用当前页面全部权限范围内订单计算，不受订单号、下单用户、手机号或订单状态筛选条件影响。订单状态以统计时点的OMS同步结果为准。

百分比公式：`趋势百分比 =（当前值 - 对比值）/ 对比值 × 100%`。

| 指标 | 当前值 | 对比值 |
| --- | --- | --- |
| 待审核订单 | 当前时点状态为待审核的订单数 | 7天前同一时点状态为待审核的订单数 |
| 处理中 | 当前时点状态为待分配仓库、待仓库拣货或待物流收揽的订单数 | 7天前同一时点三种状态的订单数之和 |
| 已发货 | 当前时点状态为已发货的订单数 | 7天前同一时点状态为已发货的订单数 |
| 本月成交额 | 本月1日00:00至当前时点，状态不为已关闭的订单金额之和 | 上月1日00:00至与当前月相同日、相同时间的订单金额之和；若上月无对应日期，取上月最后一天 |

- 前三项展示“vs 上周”，本月成交额展示“vs 上月同期”。
- 趋势保留一位小数；数学方向决定上下箭头，业务颜色可单独表达好坏，例如待审核订单下降属于正向表现。
- 对比值为0且当前值为0时展示 `0.0%`；对比值为0且当前值大于0时展示“新增”。
- 当前页面显示的趋势百分比为原型演示值，正式环境由订单状态历史快照和订单金额流水计算。
