# 独立站 - 订单管理页 PRD

## 1. 页面定位

订单管理页面向管理端运营人员，用于集中查看对公用户提交的采购订单，并完成确认、拒绝、发货和详情追踪。

页面入口位于左侧导航“管理端 > 订单 > 订单管理”。从用户管理或“我的订单”进入时，可以通过 URL 中的用户名参数自动筛选对应用户订单。

## 2. 页面结构

页面由以下区域组成：

1. 顶部栏：页面标题和身份切换入口。
2. 统计区：待处理订单、处理中、已发货、本月成交额。
3. 筛选区：订单号、下单用户或公司、订单状态。
4. 订单列表：展示订单核心信息和可执行操作。
5. 订单处理弹窗：确认、拒绝或标记已发货。
6. 订单详情弹窗：展示SKU、费用、收货信息和物流跟踪。

## 3. 顶部统计

| 指标 | 统计口径 |
| --- | --- |
| 待处理订单 | 状态为“待确认”的订单数量 |
| 处理中 | 状态为“已确认”或“拣货中”的订单数量 |
| 已发货 | 状态为“已发货”、正在等待签收的订单数量 |
| 本月成交额 | 本月所有非取消订单的订单金额合计 |

每张统计卡片左上角同排展示放大后的指标标题和小字号口径说明，主体数字使用大字号突出。趋势箭头、变化比例和对比周期位于数字同一行右侧；待处理订单下降使用健康趋势颜色，其余增长指标使用上升趋势颜色。内容区域较窄时保留趋势方向和百分比，隐藏较长的对比周期。

订单状态发生变化后，统计数据立即重新计算。

## 4. 筛选区

| 筛选项 | 控件 | 匹配规则 |
| --- | --- | --- |
| 订单号 | 文本输入框 | 支持订单号部分匹配，不区分大小写 |
| 下单用户/公司 | 文本输入框 | 同时匹配用户名和公司名称，支持部分匹配 |
| 订单状态 | 下拉选择 | 全部、待确认、已确认、拣货中、已发货、已签收、已取消 |

交互规则：

- 多个筛选条件之间为“且”关系。
- 点击“搜索”或在筛选输入框按 Enter 后刷新列表并回到第一页。
- 点击“重置”清空全部筛选条件并回到第一页。
- 从其他页面携带用户名进入时，自动填入用户条件并执行筛选。

## 5. 订单列表

| 字段 | 展示内容 |
| --- | --- |
| 订单号 | 系统生成的唯一订单编号 |
| 下单用户 | 用户名和所属公司，两行展示 |
| 下单时间 | 订单提交时间 |
| SKU明细 | 首个SKU名称、SKU种类数和商品总件数，点击可查看详情 |
| 订单金额 | 商品金额、操作费和物流费合计，美元保留两位小数 |
| 订单状态 | 使用不同颜色区分当前处理阶段 |
| 物流单号 | 已发货后展示运单号，未生成时显示“-” |
| 操作 | 根据订单状态展示确认、拒绝、标记已发货和查看详情 |

列表每页展示8条订单。分页区展示结果总数、上一页、当前页和下一页。

## 6. 订单状态

| 状态 | 含义 | 可执行操作 |
| --- | --- | --- |
| 待确认 | 新订单尚未完成库存确认 | 确认、拒绝、查看详情 |
| 已确认 | 库存已预占，等待仓库处理 | 标记已发货、查看详情 |
| 拣货中 | 仓库正在拣货或打包 | 标记已发货、查看详情 |
| 已发货 | 包裹已出库，等待签收 | 查看详情 |
| 已签收 | 客户已签收 | 查看详情 |
| 已取消 | 订单被拒绝或取消 | 查看详情 |

状态流转主路径为：`待确认 → 已确认 → 拣货中 → 已发货 → 已签收`。

拒绝订单后状态变为“已取消”，不再进入后续履约流程。

## 7. 确认订单

- 仅“待确认”订单展示“确认”操作。
- 点击后打开确认弹窗，说明确认后会预占库存并推送 OMS 创建包裹。
- 点击弹窗确认后，订单状态变为“已确认”。
- 操作成功后刷新列表和顶部统计，并显示顶部 Toast。

## 8. 拒绝订单

- 仅“待确认”订单展示“拒绝”操作。
- 拒绝原因必填，未填写时保持弹窗打开并显示警告 Toast。
- 确认拒绝后，订单状态变为“已取消”，已预占库存需要释放。
- 拒绝原因写入订单记录，供详情和后续审计使用。

## 9. 标记已发货

- “已确认”和“拣货中”订单展示“标记已发货”。
- 物流单号必填，未填写时保持弹窗打开并显示警告 Toast。
- 确认后订单状态变为“已发货”，列表显示物流单号。
- 顶部处理中和已发货统计同步更新。

## 10. 订单详情

订单详情弹窗包含以下内容：

### 10.1 基础信息

- 下单用户和公司。
- 下单时间。
- 当前订单状态。
- 物流方式和物流单号。
- 收货人、联系电话和完整收货地址。

### 10.2 SKU明细

| 字段 | 说明 |
| --- | --- |
| SKU | 商品图片、商品名称、规格和SKU编码 |
| 单价 | 下单时的SKU单价 |
| 数量 | 当前SKU购买数量 |
| 小计 | 单价乘以数量 |

### 10.3 费用明细

- 商品金额。
- 操作费。
- 物流费。
- 订单金额。

订单金额计算公式为：`商品金额 + 操作费 + 物流费`。

### 10.4 物流跟踪

按订单状态展示订单已提交、订单已确认、仓库拣货、包裹已发货和客户已签收节点。已完成节点使用绿色标识。

## 11. 页面反馈

- 业务操作结果统一使用顶部 Toast。
- 必填信息缺失时使用警告 Toast。
- 详情和操作弹窗可通过关闭按钮、点击遮罩或按 Escape 关闭。
- 黄色“查看注释”按钮默认显示，支持拖动和记忆位置；注释面板默认打开且宽度可调。
