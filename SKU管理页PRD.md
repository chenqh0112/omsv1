# 独立站 - SKU 管理页 PRD

## 1. 页面定位

SKU管理页用于查看当前身份权限范围内的 SKU 库存，集中呈现库存规模、库存状态、可用情况、组织归属和库龄健康度，并提供组织及 SKU 维度的查询与导出能力。

页面入口位于左侧导航“管理端 > 库存 > 库存管理”。进入页面后，默认展示当前身份可查看的全部 SKU 数据。

页面右侧注释区域默认打开，注释内容直接加载本 PRD。用户可以收起注释，也可以拖动边缘调整注释区域宽度。

## 2. 数据来源

SKU管理页库存数据来源于 OMS 库存查询接口：`/oms/api/inventory`。

OMS 为 SaaS 系统，页面根据登录用户所属的公司、业务组和业务员关系确定数据范围。原型使用右上角身份切换入口模拟不同权限；测试和正式环境必须以后端返回的用户权限为准。

当前原型全局生成100条 SKU 数据，用于验证筛选、权限、库龄与真实分页；列表每页展示10条。

## 3. 用户与数据权限

| 身份 | 可见数据范围 | 组织筛选项 | “所属”列内容 |
| --- | --- | --- | --- |
| 超管 | 所有公司、业务组和业务员的数据 | 公司、业务组 | 公司、业务组、业务员 |
| 公司A管理员 | 公司A下的全部数据 | 业务组 | 业务组、业务员 |
| 公司A业务组A管理员 | 公司A业务组A下的全部数据 | 业务员 | 业务员 |
| 公司A业务组A业务员A | 业务员A自己的数据 | 无 | 不显示“所属”列 |

切换身份后，以下内容同步更新：

- 概览区数字和占比。
- 组织筛选项及可选值。
- 表格“所属”列的显示层级。
- SKU库存列表及结果条数。
- 库龄分布和健康 SKU 占比。

## 4. 页面结构

页面由四个区域组成：

1. 概览区：展示 SKU总数、已上架总数、运输中总数、可用总数。
2. 筛选区：展示当前身份可用的组织筛选项、SKU编码、SKU名称和操作按钮。
3. SKU库存列表：展示聚合后的 SKU 信息、所属信息、最大库龄和库存数量。
4. 库龄分布：按 SKU 最大库龄统计四个区间，并显示健康 SKU 占比。

页面主体固定在当前视口内，不使用整页纵向滚动。SKU库存列表正文独立滚动，分页始终固定在表格底部。主工作区空间充足时，库龄分布位于表格右侧；注释打开导致主工作区变窄时，库龄分布切换为表格下方的紧凑布局。

## 5. 概览区

概览区包含四个指标模块。每个模块左侧展示指标名称和数字，右侧展示占比与周趋势。

| 指标 | 数值口径 | 占比计算方式 | 数字颜色 | 当前趋势示例 |
| --- | --- | --- | --- | --- |
| SKU总数 | 当前身份权限范围内的 SKU 数量 | 当前身份可见 SKU 数 / 全部 SKU 数 | 蓝色 | ↑12% vs 上周 |
| 已上架总数 | 当前身份权限范围内所有 SKU 的已上架数量之和 | 已上架数量 / 当前身份可见库存总数量 | 绿色 | ↑8% vs 上周 |
| 运输中总数 | 当前身份权限范围内所有 SKU 的运输中数量之和 | 运输中数量 / 当前身份可见库存总数量 | 橙色 | ↓5% vs 上周 |
| 可用总数 | 当前身份权限范围内所有 SKU 的可用数量之和 | 可用数量 / 已上架数量 | 紫色 | ↑6% vs 上周 |

### 5.1 占比展示

- 占比统一展示为“占比 X.X%”。
- 占比保留一位小数。
- 占比右侧显示问号图标。
- 鼠标移入或键盘聚焦问号图标时，显示当前指标的占比计算方式。
- 分母为 0 时，占比展示为 `0.0%`。

### 5.2 趋势展示

- 上升趋势使用绿色和向上箭头。
- 下降趋势使用红色和向下箭头。
- 文案格式为“↑X% vs 上周”或“↓X% vs 上周”。
- 趋势右侧显示问号图标。
- 趋势提示内容为：与上周同期相比，计算公式为“（本周值 - 上周值）/ 上周值”。
- 原型中的趋势百分比为演示数据，实际环境由接口返回本周值和上周同期值后计算。

概览区数据只随身份权限变化，不随搜索条件变化。

## 6. 筛选区

### 6.1 筛选项

| 身份 | 从左到右的筛选项 |
| --- | --- |
| 超管 | 公司、业务组、SKU编码、SKU名称 |
| 公司A管理员 | 业务组、SKU编码、SKU名称 |
| 公司A业务组A管理员 | 业务员、SKU编码、SKU名称 |
| 公司A业务组A业务员A | SKU编码、SKU名称 |

每个筛选项采用“上方为筛选名称、下方为筛选控件”的布局。

### 6.2 筛选规则

- 公司、业务组、业务员支持下拉搜索和多选。
- 组织选项只允许出现当前身份权限范围内的数据。
- SKU编码支持部分匹配，不区分大小写。
- SKU名称支持部分匹配，不区分大小写。
- 同一个多选筛选项中的多个选项为“或”关系。
- 不同筛选项之间为“且”关系。
- 输入 SKU编码或 SKU名称时，列表实时应用当前条件。

### 6.3 操作按钮

| 操作 | 页面行为 |
| --- | --- |
| 搜索 | 按当前全部筛选条件刷新列表、结果条数和库龄分布 |
| 重置 | 清空组织、SKU编码和SKU名称条件，恢复当前身份下的默认数据 |
| 导出 | 导出当前筛选结果，完成后显示全局 Toast“导出成功” |

## 7. SKU库存列表

### 7.1 字段顺序

表格字段从左到右依次为：

1. SKU
2. 所属，根据身份决定是否显示
3. 库龄
4. 已上架
5. 运输中
6. 已入库未上架
7. 可用
8. 锁定
9. 总数量

### 7.2 SKU 聚合列

SKU图片、SKU名称、SKU规格和SKU编码聚合到“SKU”列中。

单元格左侧展示商品图片，右侧从上到下依次展示：

1. SKU名称
2. SKU规格
3. SKU编码

展示规则：

- SKU名称使用主要文字样式。
- SKU规格使用次要文字样式。
- SKU编码使用等宽字体和弱化颜色。
- 商品图片尺寸大于项目原基础表格缩略图尺寸。
- 点击商品图片打开大图预览。
- SKU列和所属列使用紧凑间距，避免两列之间出现大面积空白。

### 7.3 所属聚合列

公司、业务组和业务员信息聚合到“所属”列中，每个组织层级占一行。

单元格只展示实际公司、业务组或业务员名称，不显示“公司”“业务组”“业务员”等字段标签。

显示顺序固定为：

1. 公司
2. 业务组
3. 业务员

页面根据当前身份隐藏已知或无权查看的上级层级：

- 超管显示公司、业务组、业务员。
- 公司A管理员显示业务组、业务员。
- 公司A业务组A管理员显示业务员。
- 公司A业务组A业务员A不显示“所属”列。

### 7.4 库存字段定义

| 字段 | 定义 |
| --- | --- |
| 库龄 | 当前 SKU 全部入库记录中的最大库龄，单位为天 |
| 已上架 | 已完成入库并放置到货架的库存数量 |
| 运输中 | 已发出但尚未到达仓库的库存数量 |
| 已入库未上架 | 已到达仓库并完成入库，但尚未上架的库存数量 |
| 可用 | 当前可以用于销售或分配的库存数量 |
| 锁定 | 已被订单或其他业务占用、暂不可分配的库存数量 |
| 总数量 | 已上架、运输中、已入库未上架三类数量之和 |

可用和锁定属于库存可用性维度，不重复计入总数量。

### 7.5 表头说明

“已上架、运输中、已入库未上架、可用、锁定、总数量”表头右侧显示问号图标。

- 鼠标移入图标时显示对应字段定义。
- 图标支持键盘聚焦。
- 图标不触发表格排序或其他表格操作。

### 7.6 库龄明细

- 一个 SKU 可以存在多次入库记录。
- 表格默认显示该 SKU 所有库龄记录中的最大天数。
- 鼠标移入库龄值或使用键盘聚焦库龄值时，显示库龄记录浮层。
- 浮层每条入库记录显示一行，字段为“库龄、上架日期、统计日期”。
- 多条记录按库龄从大到小排列。
- 没有库龄记录时显示“-”，不展示明细浮层。

最大库龄标签颜色：

| 最大库龄 | 展示样式 |
| --- | --- |
| 0-15天 | 绿色标签 |
| 16-30天 | 黄色标签 |
| 31天及以上 | 红色标签 |

## 8. 库龄分布

库龄分布按当前列表中每个 SKU 的最大库龄统计，因此会随身份和筛选结果同步变化。

| 区间 | 颜色 | 统计规则 |
| --- | --- | --- |
| 0-7天 | 深绿色 | 最大库龄为 0 至 7 天的 SKU 数量 |
| 8-15天 | 浅绿色 | 最大库龄为 8 至 15 天的 SKU 数量 |
| 16-30天 | 黄色 | 最大库龄为 16 至 30 天的 SKU 数量 |
| 30天+ | 红色 | 最大库龄大于 30 天的 SKU 数量 |

模块包含以下信息：

- 健康 SKU 占比。
- 四个库龄区间的横向分布条。
- 每个区间对应的 SKU 数量。
- 库龄统计口径提示图标。

健康 SKU 指最大库龄不超过 15 天的 SKU。计算公式为：

`健康 SKU 占比 =（0-7天 SKU 数 + 8-15天 SKU 数）/ 当前列表 SKU 总数`

当前列表为空时，健康 SKU 占比展示为 `0%`，四个区间数量均展示为 `0个`。

## 9. 表格滚动与分页

- 页面主体高度不超过当前视口。
- 表格正文使用内部纵向滚动，滚动表格时概览区、筛选区和分页位置不变化。
- 表格字段超出可用宽度时使用内部横向滚动。
- 表格头部展示当前结果条数。
- 分页固定在表格底部，始终处于当前页面可视区域。
- 工作区较窄时，分页保留总条数、上一页、当前页和下一页，隐藏每页条数及跳页控件。
- 当前原型每页默认展示 10 条数据。

## 10. 页面交互与数据联动

| 触发操作 | 联动结果 |
| --- | --- |
| 切换身份 | 更新概览、组织筛选、所属列、列表、结果条数和库龄分布 |
| 执行搜索 | 更新列表、结果条数和库龄分布，不改变概览区 |
| 重置筛选 | 恢复当前身份下的默认列表和库龄分布 |
| 移入或聚焦概览问号 | 显示占比或趋势计算方式 |
| 移入或聚焦库存表头问号 | 显示库存字段定义 |
| 移入或聚焦库龄值 | 显示该 SKU 的全部库龄记录 |
| 点击 SKU 图片 | 打开图片预览 |
| 点击导出 | 导出当前筛选结果并显示 Toast“导出成功” |

## 11. 页面状态与反馈

- 页面初始化时展示当前身份权限范围内的全部数据。
- 没有匹配数据时，表格显示“暂无数据”，结果条数显示为 0。
- 空列表下库龄分布同步归零。
- 项目内操作反馈统一使用全局 Toast，不使用浏览器弹窗或页面独立提示组件。
- Toast 在页面顶部居中显示，并在 2.5 秒后自动消失。
- SKU 导出完成后的 Toast 文案固定为“导出成功”。

## 12. 接口字段与数据关系

库存列表至少需要以下字段：

| 字段 | 用途 |
| --- | --- |
| `img` | SKU商品图片 |
| `barcode` | SKU编码 |
| `name` | SKU名称 |
| `spec` | SKU规格 |
| `company` | 所属公司 |
| `businessGroup` | 所属业务组 |
| `salesperson` | 所属业务员 |
| `shelved` | 已上架数量 |
| `transit` | 运输中数量 |
| `receivedNotShelved` | 已入库未上架数量 |
| `available` | 可用数量 |
| `locked` | 锁定数量 |
| `total` | 总数量 |
| `ageRecords` | 库龄记录数组 |

每条库龄记录至少包含：

| 字段 | 含义 |
| --- | --- |
| `days` | 统计日期减去上架日期所得天数 |
| `shelvedDate` | 本批库存完成上架的日期 |
| `statisticsDate` | 本条库龄数据的计算基准日期 |

接口还需要提供或支持以下数据关系：

- 超管账号可查询全部公司的 SKU 数据。
- SKU 与公司、业务组和业务员之间存在可查询的归属关系。
- 组织筛选接口返回值受当前登录用户权限限制。
- 概览趋势需要本周值与上周同期值，或直接返回已计算的趋势百分比。
- 库龄记录可以由库存查询接口直接返回，也可以通过 SKU 标识关联独立的库存批次接口。
