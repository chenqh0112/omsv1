# 独立站 - 入库单页 PRD

## 1. 页面定位

入库单页面向仓库及库存运营人员，用于创建不同来源的入库任务、查看头程和到仓进度，并追踪每张入库单的 SKU 明细。

当前页面作为辅助业务页面存在，页面标题为“入库管理”，仓库标识为“美西仓”。

该页面当前不在左侧主导航中展示，可通过 `inbound.html` 直接访问。

## 2. 页面结构

页面由顶部栏、入库概览、入库类型 Tab、入库单卡片列表、新建入库单弹窗和页面注释组成。

## 3. 入库概览

| 指标 | 展示内容 |
| --- | --- |
| 今日入库单 | 今日创建或处理的入库单数量，并展示较昨日变化 |
| 头程在途 | 当前头程运输中的入库单数和商品件数 |
| 待收货 | 到仓后等待收货处理的入库单数和件数 |
| 本月已上架 | 本月完成上架的商品件数和趋势 |

## 4. 入库类型

| Tab | 数据范围 |
| --- | --- |
| 全部 | 全部入库单 |
| 补货入库 | 来自斑马补货流程的入库单 |
| 外采入库 | 来自供应商采购的入库单 |
| 一件代发 | 一件代发业务产生的入库单 |
| 中转入库 | 中转业务产生的入库单 |

Tab 名称右侧展示对应数量。切换 Tab 后刷新入库单卡片；无结果时显示“暂无入库单”。

## 5. 入库单卡片

每张卡片展示入库单号、类型、来源单号、转单号、卖家、SKU明细、总件数、创建时间、预计到货时间和当前状态。

点击详情入口后，在卡片内展开物流或仓库处理信息，包括进度时间线、追踪信息和各节点时间。再次点击可收起详情。

## 6. 新建入库单

点击顶部“新建入库单”打开弹窗。

| 字段 | 规则 |
| --- | --- |
| 入库类型 | 补货入库、外采入库、一件代发入库、中转入库 |
| 来源单号 | 必填；提示文字随入库类型变化 |
| 转单号 | 选填；已有转单号时可用于头程在途跟踪 |
| 卖家 | 内部团队A、内部团队B、内部团队C、外部-蜂鸟、外部-跨境达人 |
| SKU明细 | 选择SKU并填写数量，可添加多行 |
| 预计到货 | 日期选择 |
| 备注 | 选填文本 |

## 7. SKU明细编辑

- 选择SKU后填写数量并点击“添加”。
- 未选择SKU时显示提示 Toast“请选择SKU”。
- 数量小于1时显示提示 Toast“请输入有效数量”。
- 已添加的SKU以明细行展示，可单独删除。
- 创建入库单前至少保留一个SKU。

## 8. 创建规则

- 来源单号为空时不创建，并显示 Toast“请输入来源单号”。
- 没有SKU明细时不创建，并显示 Toast“请至少添加一个SKU”。
- 创建成功后关闭弹窗、刷新列表，并显示包含新入库单号的成功 Toast。
- 新建记录插入当前数据列表后，可继续通过入库类型 Tab 查看；当前页面不提供分页。
- 选择不同入库类型时，来源单号提示与相关字段同步更新。

## 9. 页面反馈

- 表单提示和创建结果统一使用全局 Toast。
- 弹窗支持取消和关闭。
- 右上角头像可切换原型身份，页面显示当前身份名称。
- 黄色注释按钮可拖动，注释面板默认展开且宽度可调。
